#ifndef __functional_hpp_included__
#define __functional_hpp_included__

template <typename T>
bool less (T x, T y) {
	return x < y;
}

template <typename T>
bool greater (T x, T y) {
	return x > y;
}

#endif